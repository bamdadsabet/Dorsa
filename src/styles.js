const sortByFrom = {
    backgroundColor:'white',
    position: 'fixed', 
    bottom: 0, 
    left: 0, 
    width: '100%', 
    borderRadius: '5% 5% 0 0', 
    pt:5, 
    mx:0, zIndex: (theme) => theme.zIndex.drawer + 2
}

export {sortByFrom}